const human = 'человек'
const firstletter = human.slice(0,1).toUpperCase()
console.log (firstletter)
const otherletter = human.slice(1, human.length)
console.log (otherletter)
console.log (firstletter+otherletter)